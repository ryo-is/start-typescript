type Query {
  hello: Hello
  company: Company
}

type Hello {
  res: Result
  func: String
}

type Result {
  text: String
  number: Int
}

type Company {
  department: [Department]
  info: Info
}

type Department {
  name: String
  membersCount: Int
}

type Info {
  companyAge: Int
}